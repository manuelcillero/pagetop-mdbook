[package]
name = "pagetop-mdbook"
version = "0.0.8"
edition = "2021"

authors = [
    "Manuel Cillero <manuel@cillero.es>"
]
description = """\
    Embeds the assets generated by mdBook into a PageTop application to create product or API \
    documentation, tutorials, course materials, or anything that requires a navigable presentation.\
"""
homepage = "https://pagetop.cillero.es"
repository = "https://github.com/manuelcillero/pagetop-mdbook"
license = "Apache-2.0 OR MIT"

[dependencies]
pagetop = { version = "0.0", path = "../PageTop/pagetop" }
pagetop-minimal = { version = "0.0", path = "../PageTop/pagetop-minimal" }
static-files = "0.2.3"

[build-dependencies]
pagetop-build = { version = "0.0", path = "../PageTop/pagetop-build" }
